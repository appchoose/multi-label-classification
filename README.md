# Multilabel classification

---

## Load the data

Define the parameters required to retrieve the data with Elasticsearch.

```{python}
credentials = {
    'ES_HOST': ['...'],
    'ES_USR': '...',
    'ES_PWD': '...',
    'ES_PORT': ...,
    'ES_SCHEME': '...',
}
```
